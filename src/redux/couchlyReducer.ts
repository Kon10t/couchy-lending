import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const couchlySlice = createSlice({
  name: 'couchly',
  initialState,
  reducers: {

  }
})

export const couchlyReducer = couchlySlice.reducer;